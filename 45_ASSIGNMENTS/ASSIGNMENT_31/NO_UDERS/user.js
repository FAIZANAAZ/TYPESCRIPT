"use strict";
let username = ['admin', 'laiba', 'mahnoor', 'saad', 'faiza'];
if (username.length === 0) {
    console.log('We need to find same user');
}
else {
    username = [];
    console.log('All user names have been removed.' + username.length);
}
