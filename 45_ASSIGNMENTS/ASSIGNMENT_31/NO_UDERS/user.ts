//make an array is called username and store the value =same persons name

let username :string[]=['admin','laiba','mahnoor','saad','faiza'];
//if the username = 0 print a message (We need to find same user')
if(username.length === 0){
    console.log('We need to find same user');
    //if the username = []=empty print a message : All user names have been removed.
}else{
    username=[];
console.log('All user names have been removed.'+ username.length);
}